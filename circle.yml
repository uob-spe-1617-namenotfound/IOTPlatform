machine:
    python:
        version: 3.6.0

dependencies:
    pre:
        - pip install -r api/requirements.txt

test:
    pre:
        - service mongod status || service mongod start
    override:
        - python ./api/tests.py



